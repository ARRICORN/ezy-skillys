import MainContainer from "@/Components/pricing/MainContainer";
import { Fragment } from "react";

const layout = () => {
    return (
        <Fragment>
            <MainContainer/>
        </Fragment>
    );
};

export default layout;